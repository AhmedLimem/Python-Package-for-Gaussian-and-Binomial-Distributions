from setuptools import setup

setup(name='distributions',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['distributions'],
      author='Ahmed Limem',
      author_email='ahmed.limem@etudiant-enit.utm.tn',
      zip_safe=False)
